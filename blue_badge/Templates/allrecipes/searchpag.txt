I can't get pagination to work on searchresults.html, so I'm just removing it but I'm reluctant to just completely delete it, 
so here's the html code for it. It works as it should, it's just in the views that the paginator doesn't quite work.

Can be copy/pasted back into the code later if trying to make pagination work.



<div class="pagination row" id=allpag>
    <div class='col-4'></div>
    <span class="step-links col-4">
        {% if contacts.has_previous %}
            <a href="?page=1{% if keywords %}&search={{keywords}}{% endif %}"> &laquo; first </a>
                | 
            <a href="?page={{ contacts.previous_page_number }}{% if keywords %}&search={{keywords}}{% endif %}"> previous </a>
                | 
        {% endif %}

        <span class="current">
                Page {{ contacts.number }} of {{ contacts.paginator.num_pages }} 
        </span>

        {% if contacts.has_next %}
                | 
            <a href="?page={{ contacts.next_page_number }}{% if keywords %}&search={{keywords}}{% endif %}"> next </a>
                | 
            <a href="?page={{ contacts.paginator.num_pages }}{% if keywords %}&search={{keywords}}{% endif %}"> last &raquo; </a>
        {% endif %}
    </span>
    <div class='col-4'></div>
</div>